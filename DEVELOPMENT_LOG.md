# 开发日志

#### 2026-03-31 23:03 - 移除 requirements.txt 并完全迁移至 uv 依赖管理器
- **任务表述**：项目已配置 `pyproject.toml` 并使用 `uv sync` 管理依赖，用户确认是否可删除项目根目录的 `requirements.txt`。
- **完成情况**：
  - 成功删除了根目录过时的 `requirements.txt`。
  - 同步更新了 `README.md` 的快速使用指南（修改为 `uv sync` 与 `uv run python main.py`）。
  - 更新了 `README.md` 中的项目结构树文档，充分反映了目前的 Skill 架构设计。
  - 注：Skill内部的 `requirements.txt` 被保留，以确保 Skill 的可移植性。

#### 2026-03-31 22:35 - 项目架构重组：核心逻辑整体迁移至 Skill 目录
- **任务表述**：用户要求将 md2docx-converter Skill 重构为完全自包含的可移植结构，能够直接拷贝到其他项目复用，同时不影响原有 GUI 功能。
- **完成情况**：
  - 将 `converter/`、`styles/`、`utils/`、`font/` 四个核心目录从项目根**整体迁移**（非复制）至 `.agents/skills/md2docx-converter/` 内，成为唯一来源。
  - `main.py` 增加 `sys.path.insert(0, SKILL_DIR)` 桥接，使 `ui/main_window.py` 的所有 import 无需改动即自动从 Skill 目录解析。
  - `build.py` 更新 font 数据路径和 `--paths` 参数指向 Skill 目录。
  - `test_convert.py` 更新 sys.path 指向 Skill 目录。
  - Skill 内 `scripts/convert.py` 路径变量重命名为 `SKILL_ROOT`。
  - 重写 `SKILL.md`：去除硬编码绝对路径，改为动态定位；新增移植指南、前置依赖、维护说明章节。
  - 新增 `requirements.txt`（Skill 专属依赖）和 `examples/sample.md`（测试样例）。
  - 已通过全部验证：Skill CLI 独立转换、test_convert.py、端到端模拟 GUI 转换流程均正常。

#### 2026-03-31 21:10 - 封装 MD2DOCX 为 Antigravity Skill
- **任务表述**：用户希望将现有的 MD2DOCX 转换工具封装为 Antigravity（OpenClaw）的 Skill，使 AI 能在对话中直接将生成的 Markdown 内容输出为格式化的 Word 文档。
- **完成情况**：
  - 新增 `scripts/convert.py`：独立 CLI 转换入口，不依赖 PyQt6 GUI，支持 `--input`（文件）、`--text`（文本）、`--stdin`（标准输入）三种输入方式，以及 `--style A|B` 样式选择和 `--skip-font-check` 字体跳过选项。
  - 重写 `.agents/skills/md2docx-converter/SKILL.md`：完整的 Skill 指令文档，定义触发条件、命令格式、样式选择策略（默认样式 A，AI 可建议或用户指定）、四步执行流程（准备内容 → 确定输出路径 → 执行转换 → 报告结果），以及排版能力概览和注意事项。
  - 输出路径由 AI 根据上下文自动决定（用户指定 > 工作区根目录 > 桌面兜底）。
  - 已通过 `--input` 文件模式和 `--text` 文本模式的端到端转换测试，两种样式均验证通过。

#### 2026-03-31 20:39 - 重写项目 README 文档
- **任务表述**：用户要求重写 README.md，原版存在结构混乱（方法三内容缺失、目录树错位嵌入在方法三标题下）、信息层级不清晰等问题。
- **完成情况**：全面重写了 README.md。修复了原版的结构性缺陷，重新组织了信息层级：核心特性总览（解析能力、双输入模式、智能容错、排版样式、标题映射、自动化能力）→ 快速开始（三种启动方式）→ 使用方法 → 项目结构 → 二次开发 → 依赖项。补充了依赖项清单表格和 License 声明，优化了 Badge 样式，使文档更加专业和易读。

#### 2026-03-31 17:05 - 收集全部项目字体并升级为批量自动安装
- **任务表述**：用户要求将项目中用到的所有字体都放到 `font/` 目录，确保 Mac 用户也能自动安装并保持与 Windows 完全一致的排版效果。
- **完成情况**：
  - 扫描 `docx_styles.py` 确认了 3 种必需字体：仿宋_GB2312（已有）、宋体、黑体。
  - 复制了 `simsun.ttc`（宋体）和 `simhei.ttf`（黑体）到 `font/` 目录。
  - 重写了 `utils/font_installer.py`，将单字体安装升级为基于 `FONT_LIST` 的批量遍历机制，两端自动检测缺失并静默补装。

#### 2026-03-31 16:05 - 全面升级为跨平台通用桌面应用 (Windows/macOS)
- **任务表述**：用户发现旧版本强绑定于 Windows 的系统级 API（如字体安装、文件预览、可点击程序分发），提出希望使其成为兼容 Mac 的双平台工具，同时寻找简化的跨平台快速启动捷径。
- **完成情况**：
  - **字体解耦机制**：重写了 `utils/font_installer.py`，移除 `winreg` 注册表的强制逻辑。在 Mac 下通过自动读取判定并静默存入 `~/Library/Fonts` 的方法替代了原先逻辑。
  - **跨端交互适配**：修复了 `ui/main_window.py` 里的字体样式，增加 Mac 苹方备选（`PingFang SC`）；将打开文件的系统事件由专属 `os.startfile` 重组为自适应触发 `subprocess.call`（'open'/'xdg-open'）。
  - **打包自适应与免打包捷径**：全面升级了 `build.py`。现在单个 `build.py` 在谁的系统跑，就能自动产出谁原生的端（Win是 `.exe`，Mac是 `.app`）。并且附带新增了 `run-win.bat` 和 `run-mac.command` 两个极速开箱启动的快捷脚本。

#### 2026-03-31 14:10 - 统一文件与文本模式的保存行为
- **任务表述**：用户提出希望在“文件转换”模式下，不仅是默默保存到来源目录，而是像“文本转换”一样能够在生成前弹出浏览框选择目标路径。
- **完成情况**：修改了 `ui/main_window.py` 中的 `_convert_from_file` 逻辑。去除了硬编码生成的 `save_path` 自动执行，引入 `QFileDialog.getSaveFileName` 互动对话框。同时设置了友好的 `default_save_path`，确保能够基于原始 `.md` 文件名自动预填 `.docx` 后缀，使得两套模式的工作流最终行为一致。

#### 2026-03-30 17:40 - 修正 python-docx 对于列表左缩进映射的计算谬误
- **任务表述**：用户发现代码中设置的 `UL_LEFT_INDENT = 0.52cm`，但在 Word 文档里“文本之前”（左缩进）变为了 `1.52cm`。
- **完成情况**：排查发现之前对 `python-docx` 库中 `left_indent` 属性的理解存在偏差。实际上 `left_indent` 直接 1:1 映射为 Word 界面段落设置中的“文本之前”（首行起始绝对位置），而不需要自行加上 `hanging_indent` 的偏移量。撤销了原代码中画蛇添足的加法（如 `OL_LEFT_INDENT + OL_HANGING_INDENT`），改为将配置文件常数直接赋给 `left_indent`，现已完全对齐 UI 预期。

#### 2026-03-30 17:20 - 无序列表独立使用 List Bullet 样式
- **任务表述**：用户要求无序列表单独做一个文档样式，段落参数为左缩进1cm、悬挂缩进0.41cm、段前0.5行、段后0、行距1.1倍、两端对齐。
- **完成情况**：将原 LIST_* 配置拆分为 OL_*（有序列表）和 UL_*（无序列表）两组独立参数。在 `docx_writer.py` 中新建了 'List Bullet' Word 样式，并在 `_add_list` 中根据有序/无序分发不同的样式名和缩进参数。

#### 2026-03-30 17:05 - 修复列表样式异常，统一使用 List Paragraph 样式
- **任务表述**：用户反馈有序列表和无序列表的格式显示异常，要求统一使用 Word “列表”样式，并按照附件截图配置段落参数（左缩进0.3cm、悬挂缩进0.7cm、段前0.5行、段后0、行距1.1倍、两端对齐）。
- **完成情况**：在 `styles/docx_styles.py` 中新增 LIST_* 配置常量。在 `converter/docx_writer.py` 的 `_setup_default_styles` 中创建 'List Paragraph' Word 样式并应用仿宋字体、缩进、行距、段前后等参数。重写了 `_add_list` 方法，移除了硬编码的 `Cm(1.27)` 缩进和空格前缀，改为统一使用 'List Paragraph' 样式，嵌套层级通过递增左缩进实现。

#### 2026-03-30 16:35 - 修复文本粘贴后内容截断丢失（block_text未处理）的 Bug
- **任务表述**：用户反馈粘贴的 Markdown 文本导出 Docx 时会出现不完整、大量内容被吞掉的严重问题。
- **完成情况**：诊断确认该问题并非崩溃导致，而是因为无空行的紧凑列表（Tight List）以及无空行的引用块（Block Quote）在经过 Mistune v3 解析引擎时，会被解析维特有的 `block_text` 节点（而不是常规的 `paragraph`）。由于之前的遍历引擎 `_add_list`、`_add_block_quote` 乃至主节点处理器 `_process_node` 内均硬编码过滤了只处理 `paragraph` 类型的子节点，导致所有身为 `block_text` 的大段内容惨遭静默抛弃。目前已在 `docx_writer.py` 补充修改了全部的判断逻辑（由 `== 'paragraph'` 变更为 `in ('paragraph', 'block_text')`），并顺手修复了变量未定义导致的列表序号错乱问题，内容截断现象彻底消失。

#### 2026-03-30 16:10 - 解决普通段落中因缺失空行导致生成软回车（^l）的问题
- **任务表述**：用户提出普通段落中如果没有空行，解析后会挤在同一个 Word 段落中并用软回车（^l，Shift+Enter）连接，期望能够全部转换为硬回车（^p，独立的段落）。
- **完成情况**：重写了 `converter/docx_writer.py` 中的 `_add_paragraph` 方法。改变了原来将一整个段落块中的 `softbreak` 映射为换行符（`\n`）的做法，而是先在构建层面对子节点进行切分。只要遇到 `softbreak`、`linebreak` 或文本内带有换行符，就强行截断当前组，并为每一组独立生成一个带有 `Body Text` 样式的真正的 Word 段落（`^p`）。现在即使用户粘贴的每一行文字之间都没有空行，也能完美转换出独立的段落效果了。

#### 2026-03-30 15:55 - 修复直接粘贴文本缺少空行导致的解析问题
- **任务表述**：用户提出直接粘贴的 Markdown 文本可能前后没有保留严格空行，这会导致解析器将标题与正文粘连。
- **完成情况**：在 `md_parser.py` 的解析入口中增加了正则预处理逻辑，能够自动识别 `#` 标题上方及下方的缺失空行，并在将其传入 Mistune 解析器之前进行隔离替换，大幅提高了对不规范 Markdown 格式和用户剪贴板文本的容错率。

#### 2026-03-30 15:30 - 新增直接粘贴 Markdown 文本转换功能
- **任务表述**：新增在应用内直接粘贴/输入 Markdown 文本并转换为 DOCX 的能力。
- **完成情况**：重构 `main_window.py` 为双标签页结构：「📄 文件转换」保留原有拖放/选择功能，「✍️ 文本转换」新增 QPlainTextEdit 文本输入区 + 清空按钮。转换逻辑根据当前 Tab 分发：文件模式走原路径，文本模式弹出保存对话框后直接内存转换。提取了 `_show_success_dialog` 公共方法消除重复代码。分支：`feat/paste-markdown`。

#### 2026-03-30 15:10 - 修复一级标题字体被正文字体覆盖的根因问题
- **任务表述**：用户反馈 `#` 一级标题始终显示为宋体而非黑体，请检查是否为 `heading` 配置导致。
- **完成情况**：根因并非 `heading` 配置，而是 `_add_inline_content` 方法中调用的 `_apply_body_font` 会对每个 run 强制设置仿宋字体，这个 **run 级别的覆盖** 会盖掉样式定义中的黑体。现已在 `_add_heading` 中增加了对所有 run 的字体重新赋值：一级标题使用 `FONT_CONFIG['title']`（黑体），其余标题使用 `FONT_CONFIG['heading']`（宋体）。

#### 2026-03-30 15:05 - 统一一级标题配置，样式A/B的 # 均采用黑体
- **任务表述**：1) 用户反馈标题显示为宋体而非黑体，需检查；2) 样式B 的 `#` 也应改为黑体、居中对齐、段前0段后16磅。
- **完成情况**：将 `docx_styles.py` 中的 `title_a` 配置重命名为 `title`，作为两种样式通用的一级标题配置。在 `docx_writer.py` 中通过 `self.heading_map[1]` 动态获取当前模式的一级标题样式名（A=Title，B=Heading 1），对该样式统一应用黑体/加粗/无边框/段前0/段后16磅。其余标题保持宋体不变。

#### 2026-03-30 15:00 - 样式A标题属性提升至 Word 样式定义层级
- **任务表述**：用户反馈样式A的标题段前段后等属性并未反映在 Word 样式列表中，说明是段落级硬编码而非标准样式定义。
- **完成情况**：将 `_add_heading` 中所有对样式A Title 的属性覆盖（字体、加粗、边框、段前段后）全部迁移到 `_setup_default_styles` 中，直接修改 Word 的 Title 样式对象。清理了 `_add_heading` 中的冗余代码，现在只保留字号和居中的动态应用。Word 样式面板中现已能正确显示所有配置。

#### 2026-03-30 14:55 - 样式A标题间距配置与UI布局优化
- **任务表述**：1) 样式A标题段前改为0、段后改16磅；2) 解决界面控件挤压重叠的问题。
- **完成情况**：在 `docx_styles.py` 的 `title_a` 配置中增加了 `space_before=Pt(0)` 和 `space_after=Pt(16)` 参数，并在 `docx_writer.py` 中应用。UI 方面：窗口扩大至 600x620，优化了各控件间距、单选按钮最小高度及状态栏自动换行。

#### 2026-03-30 14:45 - 增加页脚页码及样式A标题字体专属配置
- **任务表述**：1) 导出的文档需要在页脚展示“第 X 页 共 Y 页”格式的页码；2) 样式A 的一级标题（Title）应采用黑体、加粗、无下划线。
- **完成情况**：在 `docx_styles.py` 中新增了 `title_a` 字体配置块和页脚相关的 `FOOTER_PAGE_NUMBER`、`FOOTER_FONT_NAME`、`FOOTER_FONT_SIZE` 参数。在 `docx_writer.py` 中新增了 `_setup_footer()` 方法，通过 Word 域代码（`PAGE`/`NUMPAGES`）实现动态页码居中展示；同时在 `_add_heading` 中增加了对样式A一级标题的字体/加粗/下划线独立控制逻辑。

#### 2026-03-30 14:36 - 一级标题居中对齐
- **任务表述**：无论样式 A 还是 B，`#` 一级标题均应居中显示。
- **完成情况**：在 `docx_writer.py` 的 `_add_heading` 方法中增加了 `level == 1` 的判断，符合条件时对该段落强制注入 `WD_ALIGN_PARAGRAPH.CENTER` 居中属性。

#### 2026-03-30 14:33 - 样式A标题字号与样式B分级对齐
- **任务表述**：用户希望样式 A 的每级标题字号采用样式 B 的下一级（如样式A的 Title 对应样式B的 H1 字号，H1 对应 H2 字号，依此类推）。
- **完成情况**：将 `docx_styles.py` 中的单一标题字号映射表 `HEADING_FONT_SIZES` 拆分为两套独立的字号表（`HEADING_FONT_SIZES_A` 和 `HEADING_FONT_SIZES_B`），新增 `get_heading_font_sizes()` 动态返回对应样式的映射。同时修改 `docx_writer.py` 在初始化时根据样式模式获取对应字号表，并在渲染标题时从实例变量中取值。

#### 2026-03-30 14:28 - 增加清除选择按钮
- **任务表述**：用户希望在选择文件按钮旁边增加一个“清除选择”按钮，方便取消已选的文件并重置拖拽区域状态。
- **完成情况**：在 `DropArea` 中新增了 `clear_file()` 方法用于重置状态。在主界面中将“选择文件”和“清除选择”按钮并排放置，后者以红色风格视觉区分。

#### 2026-03-30 14:26 - 简化转换操作流程
- **任务表述**：用户反馈当前流程中，选择文件后点击“开始转换”仍需再次操作保存弹窗，体验不佳。希望实现：1) 已选文件后点击转换立即执行；2) 未选文件时点击转换则弹出选择框后直接开始。
- **完成情况**：重写了 `ui/main_window.py` 中 `do_convert()` 方法的整体流程。取消了强制的“保存确认”对话框，改为自动以同目录同名 `.docx` 保存。当未选择文件时，点击转换会先弹出文件选择对话框，确认后直接开始转换。同时将“开始转换”按钮默认设为始终可用状态。

#### 2026-03-30 14:21 - 增加文件占用容错拦截机制
- **任务表述**：用户在反复生成更新文档时，出现了 `[Errno 13] Permission denied` 报错，导致程序生成过程中断抛出警告。
- **完成情况**：这是典型的 Windows 系统句柄锁问题。由于之前增加了“打开文档”的便捷按钮，当在外部 Word（或其他阅读器）中正文阅览同名旧文件尚未关闭时，直接在界面点击重新转换以覆盖原文件，便会引发底层的写入权限封锁。现已在 `ui/main_window.py` 追加针对 `PermissionError` 的专属错误捕获分支，拦截异常后弹出人类可读的提醒消息，指引用户主动关闭旧有被占用文件即可恢复流转。

#### 2026-03-30 14:18 - 优化正文文本段前排版间距
- **任务表述**：对于正文文字（Body Text），用户提出希望进一步剥离其原本设置的“无段前间距”，改为统一增加“段前 0.5 行”。
- **完成情况**：在 `styles/docx_styles.py` 中将负责管控的硬编码 `BODY_SPACE_BEFORE` 常量剥离，替换上了基于标准底层 XML 格式管控的 `BODY_SPACE_BEFORE_LINES = 50`。随后修改主渲染生成器 `docx_writer.py` 内部 `get_or_add_spacing` 的注入机制。现在针对“正文文本”生成的内容，段落前端自带 0.5 行距且在 Word 段落栏展示所见即所得。

#### 2026-03-30 14:15 - 修复由衍生字符样式导致的渲染报错
- **任务表述**：继续修复了用户在生成 DOCX 时遇到的致命错误：`'CharacterStyle' object has no attribute 'paragraph_format'`。
- **完成情况**：复现发现，由于在 Word 系统内部，“标题 1 (Heading 1)” 除了作为段落样式存在外，往往会衍生出一个名为“Heading 1 Char”的字级字符样式 (CharacterStyle)。由于这两者在内部系统池里共用 Heading 作为前缀，所以在我们遍历大纲强行注入设置属性时，非段落的“Heading 1 Char”被拦截并意外尝试了提取不存在的“段落格式(paragraph_format)”。现已在 `docx_writer.py` 的匹配过滤逻辑中加入底层类型判定 `s.type == WD_STYLE_TYPE.PARAGRAPH`，完美拦截了此崩溃。

#### 2026-03-30 14:13 - 重构样式参数架构，实现单点精准映射
- **任务表述**：用户提出将所有分散在这两次修改中关于标题、正文文本、正文等在生成器节点硬编码设定的格式属性，全部回归并严丝合缝地在 `styles/docx_styles.py` 中进行一一匹配管理。
- **完成情况**：在 `docx_styles.py` 中重写了完整的段落排版属性块（含 Normal/Body/Heading），拆分提取了单独控制字号、行距及 XML w:XXXLines 的属性大类。随后在 `docx_writer.py` 的初始化函数体内抹清了全部数字硬编码，转为通过 `from docx_styles import` 完全拉平获取依赖。达到了数据与代码呈现逻辑剥离的系统标准。

#### 2026-03-30 14:10 - 进一步精细化全局行距
- **任务表述**：剥离所有非必要的默认行距参数。1) “正文”行距归零（单倍行距，无段前段后）；2) “所有标题”内部重置为行距零（单倍行距）；3) 独立配置最终承载内容的“正文文本”行距为 1.2 倍。
- **完成情况**：在 `docx_writer.py` 的默认样式装载方法 `_setup_default_styles` 中分别对 `Normal`、`Body Text` 及所有 `Heading` 样式节点重新明确了 `line_spacing` 参数。其中为满足“无行距”需求，对正文与标题下发了 `line_spacing = 1.0`（系统意义上的紧凑单倍计算无多倍扩充），并独立约束了段前段后为 `Pt(0)` 以免默认被撑开。

#### 2026-03-30 14:08 - 修复按行设定的段前段后间距报错
- **任务表述**：用户在点击转换时程序抛出了异常报错 `'CT_Style' object has no attribute 'get_or_add_spacing'`。
- **完成情况**：经排查发现 `python-docx` 库中定义在“大纲样式”(Style) 上的段落格式对象与其他普通段落不同，无法直接调用间距函数。已修改 `docx_writer.py` 的代码逻辑，将调用层级修正为深入到 XML Element (`pPr`) 内部获取并写入 `spacing`，彻底修复了该报错。

#### 2026-03-30 14:05 - 修复文件选择后意外触发自动转换的焦点 BUG
- **任务表述**：用户点击“选择 Markdown 文件”并在弹窗确认后，程序不应该马上开始转换，而应该由用户手动点击“🚀开始转换”引发事件。
- **完成情况**：经排查是由基于 PyQt6 底层在弹窗中使用回车（Enter键）确认时，抬起事件（Key Release）意外透传到了主界面刚刚被启用的默认焦点按钮（即“开始转换”按钮）引发。现在已经在 `ui/main_window.py` 取消了转换按钮对任何系统焦点的自动捕获（`Qt.FocusPolicy.NoFocus`）并在弹窗前后进行主动的焦点清空，成功阻断了该回车键透传问题。

#### 2026-03-30 14:02 - 优化正文对齐方式与定制标题纵向行跨度
- **任务表述**：用户希望生成的文档正文具有“两端对齐”属性以确保版面整洁；同时，文章内部涉及的所有大纲标题段落需符合特定的字号行距设定：即段前 1 行、段后 0.5 行。
- **完成情况**：在底层操作对象 `docx_writer.py` 的初始化段中，对 `Normal` 及 `Body Text` 双双注入了枚举属性 `WD_ALIGN_PARAGRAPH.JUSTIFY`。针对各个级别的标题渲染逻辑，取消了易错的绝对磅值，通过改写 OpenXML 底层标签 `<w:spacing w:beforeLines="100" w:afterLines="50" />` 完美实现了由 Word 自主根据缩放分辨率实时计算的标准 1行/0.5行间距效果。

#### 2026-03-30 13:52 - 增加转换成功后的立刻打开原文档入口
- **任务表述**：更改转换成功的消息提示框设计，追加一个「打开文档」的独立按钮，点击后自动调用系统关联软件打开生成的 Word 文件。
- **完成情况**：重构了 `ui/main_window.py` 内通过 `QMessageBox` 弹出完成提示的逻辑，增加了 `Open File` 定制按钮与相关判定分支，通过跨平台 API (`os.startfile` 及相应 OS 原生呼叫子进程命令) 实现了转换后的无缝调取。

#### 2026-03-30 13:50 - 精细化调整正文间距及统一标题样式
- **任务表述**：用户需求剥离正文默认附加的行间距与段前段后属性，同时要求全篇所有的级联标题统一变为“宋体”且字体颜色强制重置为自动（黑色）。
- **完成情况**：在 `docx_styles.py` 中将正文相关的间距变量设为 `None`，并在 `docx_writer.py` 应用段落时增加空值规避以维持原有无间距状态；同时在初始化配置方法 `_setup_default_styles` 及具体标题生成的游标操作（Runs）中双重注入了清除原本蓝色字体的黑色色值（RGB: 0, 0, 0）与“宋体”映射参数。

#### 2026-03-30 13:35 - 修复段落缩进脱离样式库管理的问题
- **任务表述**：用户反馈 Word 界面中“正文文本”等样式未显示缩进，但实际导出的段落却被强制缩进了。
- **完成情况**：优化了 `docx_writer.py` 的段落生成逻辑，移除了之前硬编码直接向每个段落对象追加缩进属性的做法。转而在初始化时标准地将首行缩进、段前段后、行间距配置绑定至原生的“正文文本”(Body Text) 样式上，并强制所有 Markdown 正文通过挂载该样式生成内容，从而实现了所见即所得。

#### 2026-03-30 13:25 - 全局关闭文档网格对齐与自动缩进配置
- **任务表述**：禁用所有段落格式中的“如果定义了文档网格，则自动调整右缩进”与“如果定义了文档网格，则与网格对齐”的功能，消除行间距被网格吸附控制的视觉差。
- **完成情况**：在 `docx_writer.py` 的默认样式装载方法 `_setup_default_styles` 中遍历整个 `self.doc.styles`，通过底层操作 OpenXML 的 `<w:snapToGrid w:val="0"/>` 及 `<w:adjustRightInd w:val="0"/>` 节点注入，彻底关闭这两项属性拦截。

#### 2026-03-30 13:17 - 修改正文排版样式与添加字体自动静默安装机制
- **任务表述**：更改正文字体为“仿宋_GB2312”（小四），首行缩进2字符，段前段后0.5行，行间距1.2倍。若目标环境无此字体，需自动实现安装功能。
- **完成情况**：
  1. 修改 `styles/docx_styles.py` 中的格式设定（`FONT_CONFIG`, 间距, 小四, 行距1.2倍），并在 `docx_writer.py` 写入段落时应用首行缩进（24pt/2字符）。
  2. 新增 `utils/font_installer.py`，实现在应用程序 `main.py` 启动入口自动检查所需字体。若不存在，则将自带的在本地的 `font/仿宋_GB2312.ttf` 拷贝给用户的 `LOCALAPPDATA` 字体目录并写入注册表以实现免管理员权限静默安装。
  3. 更新 `build.py` 以合并 `font/` 资源目录到最终分发的 `.exe` 可执行文件中。
